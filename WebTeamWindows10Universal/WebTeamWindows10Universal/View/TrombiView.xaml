<Page
    x:Class="WebTeamWindows10Universal.View.TrombiView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WebTeamWindows10Universal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:WebTeamWindows10Universal.ViewModel"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.DataContext>
            <viewmodels:TrombiViewModel />
        </Grid.DataContext>
        
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="IDTB" Width="150" Text="UserID" Margin="0 0 10 0"/>
                <Button Content="Rechercher l'utilisateur" Command="{Binding LookForUserCommand}" CommandParameter="{Binding Text, ElementName=IDTB}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
