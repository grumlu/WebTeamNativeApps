<Page
    x:Class="WebTeamWindows10Universal.View.FUSEView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WebTeamWindows10Universal.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:WebTeamWindows10Universal.ViewModel"
    mc:Ignorable="d">
    
    <Grid>
        <Grid.DataContext>
            <viewmodels:FUSEViewModel />
        </Grid.DataContext>

        <StackPanel Margin="20 20 0 0">
            <TextBlock Text="{Binding Title}" Style="{StaticResource HeaderTextBlockStyle}"/>

            <!--<MediaElement x:Name="fusePlayer" Height="15" Width="150"/>-->
            <StackPanel Orientation="Horizontal" >
                <Button Content="Play" Command="{Binding PlayCommand}" />
                <Button Content="Pause" Command="{Binding PauseCommand}"/>
                <Button Content="Stop" Command="{Binding StopCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
